Serial.begin(9600);      // Start serial communication at 9600 baud.
Serial.end();            // Stop serial communication.
Serial.available();      // Return how many bytes are waiting to be read.
Serial.read();           // Read one incoming byte.
Serial.print(value);     // Print text or a value without a new line.
Serial.println(value);   // Print text or a value, then move to a new line.
